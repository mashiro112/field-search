> 历史文档共享副本 · 发布于 2026-09-17。保留当时的结论、失败、限制和计划；旧授权、自动续接、模型配置与“下一步”不作为当前执行指令。科研默认流程以 FS03 为准。已移除内部任务标识、替换本机路径并调整链接；没有重新运行历史验证。

# FS01 持续路线图

使命：统一field-search入口按实际任务选择成熟搜索能力，向工作任务交付可验证、可行动上下文；用户无需逐个考察和选择工具。具体状态与关系只读FS01 canonical，本文件保存版本方向、证据缺口和最小实验。

## 最新阶段：FS01 通用优化恢复（2026-09-09）

科研分支已由独立 FS02 承接。用户已恢复本窗口通用优化，并同意 Home 定向、Luna 执行检索/工具/实施、Critic 按需独立验收的低用量分工。当前 E2 核查常驻上下文中可按需读取的特定细节及重复内容，净收益成立才最小修改；不默认部署新工具、不恢复旧候选。下节为科研分支的历史暂停记录，不再暂停 FS01；当前执行状态以 canonical 为准。

## 当前阶段：科研增强规划，实施暂停（2026-09-09）

已分析采纳 Pro 最新建议。目标、轻量概念、行为规格和六项验收统一见 docs/work-items/FS01-WORK-ITEM.md 的“当前优先边界”。优先研究问题推进与证据比较，保留探索和资源复用；不预设数据集接口、平台或工具安装。当前不派发实施，定时续接不得恢复下文历史下一步。用户要求进入实施后，再从真实任务辨认能力缺口。

## 已验收基线

FS01-v4.1（2026-09-07）：recent.py调用last30days选定免费完整引擎，document.py提供全文快照/分页/离线查找。T1核心条件持平、T2互补集成、H1核心无退化；未证明H1自然触发新增脚本、普遍优势或速度优势。证据：RESULTS.md、evidence/independent-review.md、release-manifest.json。保留原交付包，不将本次使命扩展追溯为旧版验收失败。

## 历史R4至R7.1：动态网页与结构化证据

当前固定配置（2026-09-08迁移后，覆盖全部历史模型约定）：Home gpt-6-astra/low；Execution gpt-5.6-luna/max；Critic gpt-6-astra/low。禁止自动升降档或沿用旧默认。跨任务消息一律省略model/thinking以保持接收任务已有配置，除非用户再次明确修改。

Home只读简洁结果、实际增量、阻塞和下一步，按需展开证据，不重复E/C工作；机械核对复用已有脚本。Critic一次列清实质返修，复审只看变更及受影响部分。保留已通过验证与如实标注的历史缺失，减少反复全文读取、轮询、回执和纯措辞返修。每轮锁定一个可检验且值得投入的能力目标，连续无实质进展时复查方法或切换方向，不扩大无效测试。当前轮次以canonical及本页最新接管段为准，不另开重复轮次；长期使命保持活动。

2026-09-08：复用 E1（当前Luna/high）实跑此前只核对原件的 Crawl4AI。隔离部署后，以一个公开动态页面与一个静态对照核验浏览器渲染、正文抽取、来源保全、失败/超时和资源释放；先记录现有读取缺口，有恢复增量才按现有 search/document 模式最小接入。演示站点评测不冒充宿主自然发现。初轮仅两页和针对性检查，部署故障查因后最多一次有依据替代；不重复无效尝试。具体版本由现有 C1（当前Terra/high）独立验收，Home持续处理返修与下一项。

R4 限定验收（2026-09-08）：C1 独立 PASS，复算46/46冻结证据无漂移。接受隔离 Crawl4AI 0.9.3 候选的公开两页浏览器读取、受控等待失败/清理、显式 `--reader crawl4ai` 与保存后离线 `open/find`；`auto` 保留native/Jina。占位符仅在渲染HTML出现、浏览器缓存路径需显式配置、候选运行时曾需既有websearch路径均保留为限制。R4.1将把已验收候选部署到installed Skill并只复核部署入口、离线回放和受影响范围，随后再交C1；不扩大成新测评工程。

R4.1 部署验收（2026-09-08）：C1 独立 PASS，确认显式reader已部署至installed Skill，真实动态入口、已保存快照的离线 `open/find`、静态 `auto` 保持Jina和rollback dry-run均通过。运行依赖R4隔离解释器、既有websearch路径和明确的Playwright缓存，不能称为便携独立安装；回退 apply 前须先重核当前后置hash。下一轮选Agentic-Index实际运行公开仓库代码检索，判断当前统一入口缺少的代码级证据是否值得最小接入。

R5 负结果验收（2026-09-08）：C1 独立接受不集成 Agentic-Index。固定 commit 的原 CLI 与排序函数均实际运行，能发现并按分数排列仓库；但真实输出和网络机制均未提供代码搜索所需的路径、符号、片段或提交级匹配。R5 没有修改 installed Skill，R4.1 的后置文件哈希仍一致。R6 改为有界验证公开、免凭据的 Sourcegraph 路径，只有形成可核查的代码级增量才考虑最小接入。

R6 执行检查点（2026-09-08）：E1 已冻结一个匿名、公开且免凭据的 Sourcegraph 实跑。对 microsoft/vscode 的固定符号查询返回 repository、path、commit、symbol、行号与来源 URL；GitHub raw 对照只提供固定文件文本。响应明确给出 `shard-match-limit`，因此不得把结果表述为完整穷尽；未使用 cookie、token、私有仓、付费服务或 LLM。尚未改动 installed Skill；候选仅建议显式 `--reader sourcegraph`，不扩张 `auto`，待 C1 独立验收后决定。

R6 条件验收（2026-09-08）：C1 接受代码级结构化增量的实跑事实，但只允许保留为证据和显式候选。原查询没有 `rev:` 固定项；后续候选须传入并保留固定 revision，或明确为观测 revision。冻结材料还缺可审计的脱敏请求记录，以及能够独立核查的正式使用条款和许可证证据；故无凭据/使用边界不得过度主张，安装、分发和 `auto` 扩张均暂缓。R6.1 只补以上三个缺口，再由 C1 复审。

R6.1 复审验收（2026-09-08）：C1 接受三项补证。固定 `rev` 与 `count` 已实际进入查询，原始 SSE、commit 一致性和 `shard-match-limit` 可复查；harness 只证明该次请求无凭据，并不推断服务整体价格或授权。官方 FAQ/ToS 与固定 VS Code MIT LICENSE 已冻结，Sourcegraph 仓库许可证 URL 的 404 仍是边界。ToS 将服务/Code Data 限于内部分析并限制未经批准的再分发，因此 R6.2 只能在独立目录准备本地、显式、固定rev+count、保留来源/限额/失败的候选 reader；不得安装、启用 `auto`，或缓存后对外转发结果。

R6.2 实现退回（2026-09-08）：C1 确认实际固定案例、最小定位字段、无重试、限额和缺字段本地失败均成立，但不接受候选可复用。实现没有把返回 repo/commit/source URL 与请求的固定值比较；self-test 能绕过正常 `--out` 的审查目录和不覆盖保护；`--query` 未拒绝内嵌 repo/rev/count 控制项；audit 也未记录或禁用 proxy transport。R6.3 只修复这四项，并添加反例；在独立复审通过前继续仅作 review artifact，不安装、不外发、不启用 `auto`。

R6.3 本地候选验收（2026-09-08）：C1 接受本地、内部、人工显式调用的候选实现。返回 repository/commit/source URL 与请求固定 repo/rev 绑定；所有分支共用审查目录及不覆盖保护；冲突过滤器被拒绝；代理环境在联网前被拒绝或禁用并记录。固定案例仍为一次无凭据 GET、零重试、count=5和最小定位输出，五个无网络反例不泄露定位数据、不越界写入、不覆盖。已安装 Skill 的R4.1后置哈希未变。此接受受 R6.1 ToS 边界约束：不得发布、外发、自动运行或服务化，也不安装或扩张 `auto`；长期使命继续从许可与访问边界允许的下一项独有能力中选择。

R7.1 公开PR证据候选验收（2026-09-08）：C1 接受公开、人工显式、有限分页的 GitHub REST 字段适配路线。本机未安装 `gh`，故材料只称官方 REST 的字段适配，绝不称 GitHub CLI 实际运行。固定公开 PR 的五份原始响应、有序请求 manifest、相关响应头、bytes 与 SHA-256 已冻结；离线 verifier 不依赖 capture 或网络，仅凭快照重算 PR 状态、作者、评论数、分页和 HTML 基线。该候选只限公开目标及最多5条评论，不安装、不自动路由、不分享、不面向私有目标或扩大分页；R4.1和R6.3均未受影响。

## 已验收R3：包注册表搜索的实际增量

2026-09-08恢复：R2-v2.1限定验收保留。E1以Luna/max核验此前仅读源码的OPEN-SOURCE-SEARCH npm/PyPI组件，先排除现有能力重复，再冻结一项实际包选择任务与决定条件并实跑对照。重点检查失败是否伪装为空结果，以及注册表信息能否改变决定；有证据缺口才做最小改进，无增量允许负结果交付。不调用不明LLM链，不复制不兼容许可代码，不重跑旧轮。具体新版本由现有C1以Astra/medium独立验收，Home负责返修和采用。

R3-E1 检查点（2026-09-08）：原始 npm/PyPI 模块已按真实命令运行；PyPI 页面解析返回 HTTP 200 但 regex=0，XML-RPC 返回已弃用 Fault，两个受控失败均坍缩为 `[]`，因此不能解释为 no-match。有限选择任务选定 PyPI `trafilatura==2.2.0`（Apache-2.0、Python >=3.10、含 Python 3.12/Microsoft classifiers）；本地 454 字符 HTML smoke 保留正文并移除导航/赞助，network_used=false。现有 Skill 未改，npm/PyPI 组件未被直接集成；原始 R3 证据包曾被 C1 退回，随后由 R3.1 加性纠正包修订并独立验收，完整 OPEN-SOURCE-SEARCH 产品仍未复现。

R3.1 限定证据验收（2026-09-08）：C1 以 Astra/medium 独立 pass，确认两组返修已闭合并吸收 `RESULT-PACK-r3.1.md`。接受范围仅为上述有限包选择、原组件调用及 PyPI 失败语义；保留历史 start/freeze 时间未知、一个 native 批次未定位、registry 原始正文未保存、完整部署/依赖许可/大语料质量/成本速度因果未验证等边界。未修改 installed Skill、R2、基线或 Harness；不代表完整 OPEN-SOURCE-SEARCH 产品或新的自动路由能力通过。旧 R3 return 与原件继续保留。

## 历史R2：统一入口与自然路由

先由Execution核对所有已有候选原件和真实运行，形成候选→机制→独有能力→证据→采用/延后决定的紧凑对照，并补Grok对应关系。框架须从证据产生。随后修复真实任务中统一入口与能力选择的缺口，复用v4.1和现有项目模式，避免新通用编排平台。每条新路由须有真实执行证据；完整信息和失败状态不得因统一入口丢失。

发布门：冻结候选与预算，新任务不提示脚本名；与v4.1/适用原工具比较行动依据、决定条件、来源真实性和完整性、成本、失败行为及自然触发。Critic独立审查具体版本并返回可定位问题，Execution修复后复验。仅在全部本轮必需项通过后记录限定发布，使命继续开放。

R2启动检查点（2026-09-07）：E1已逐项核对157个v4.1文件并复制到 `runs/r2/baseline-v4.1/`；冻结实验见 `runs/r2/EXPERIMENT.md`，基线清单SHA为 `304de7bf9e256dd3171eba202869a0e0c5d0dc991ed20373f6e462a80474cf29`。Home已接受本轮范围：复用search.py承接普通读取全文保全、近期整体引擎与部分失败传播；原生检索继续由Skill宿主按信息缺口选择。此为范围确认，尚未发布或验收R2。候选对应新增关注PDF中的语义代码检索和高级查询方法项。来源已确认每日10:00原生heartbeat `fs01` 指向Home；在途单轮优先续接，禁止重复创建。

## 待证实假设与后续最小实验

R2-v1审查检查点（2026-09-07）：C1正式return，见 `runs/r2/critic/FS01-R2-v1/VERDICT-PACK.md`。候选身份和131件证据核验通过，候选/Grok映射缺口已补；发布必修仍为Windows终止建议、长文统一入口自然触发、首答成功判据/资源与失败状态的独立复验。v4.1在本轮基线L1曾自然调用旧document，因此不把历史H1未触发解释为旧版永不触发。新旧模型不同、计时终点不同，不做速度/因果排名。原四题同一顺序代理与fresh协议偏离须如实记录；新验证保存真正独立启动提示。E1按用户要求Luna/max返修，优先复用有效证据，至多按缺口安排必要新验证，未发布R2。已安装候选未回滚，v4.1包仍是最后已验收恢复基线。

R2-v2审查检查点（2026-09-08）：C1第二轮仍 `return`，详见 `runs/r2/critic/FS01-R2-v2/VERDICT-PACK.md`。B1 Windows停止语义与B3针对性答案核验已闭合；B2连续两轮未由新需求触发helper，C1要求先复查验证方法/规格，停止更换“原生已足够”的Python长页；另有D1 stdout page与snapshot字段混淆、D2声称post-freeze重跑却无可定位原始日志。v2候选未验收，v4.1仍是最后验收基线。

历史返修轮（R2-v2.1方法复查+证据修正）：不再为命中helper强造长文任务。Execution先提出可观察的信息缺口判据：只有原生路径无法满足且无需预选工具的真实任务，才可检验helper自然触发；native-sufficient stop是有效路由结果，但不能证明helper自然性。Home据此决定是否提出新的限定发布范围，并由C1重审；不能把B2静默移入“已验证”。同时以新增证据包修正D1/D2：分别写清终端page和保存snapshot字段；若无v2重跑原始日志，改为明确复用v1未变代码证据，删除不可核验的重跑断言。v2历史包、封存题、v4.1基线均不覆盖。保持一个Execution写入轮次，完成后再交C1。

R2-v2.1限定发布范围（Home 2026-09-08 已采用，C1 已 conditional_pass 确认）：接受统一 `search.py` native-first 入口、native-sufficient stop、recent 完整记录/低排名/partial 保全、document 明确选用时的全文快照/失败保留/离线分页/find，以及已核验的平台/资源/超时/取消指导。B2 helper 自然发现继续是未验证长期缺口，不强造任务或暗中移项；普通任务不强制慢调用。此范围不宣称所有URL自动路由、收费/登录/完整websearch/独立研究复现、速度或因果优势、全面胜过成熟工具。Verdict见 `runs/r2/critic/FS01-R2-v2.1/VERDICT-PACK.md`；canonical已记录限定接受，长期使命保持开放。

| 假设/能力 | 价值与现有证据 | 证据缺口 | 复查触发条件 | 下一步最小实验 |
|---|---|---|---|---|
| 单一Skill入口可根据信息缺口稳定选择recent/document/原生检索 | R2-v2.1有限样本支持native-first、native-sufficient stop、N1近期入口和明确选用document组件 | helper自然发现仍未验证；宿主从零发现Skill及普遍路由可靠性不可由少量样本推出 | 真实native gap出现且已有helper适用，或新任务暴露路由错误 | 先记录原生返回的具体缺口，再观察自主选择与决定性后段证据；不强造长页命中 |
| 候选共通机制能减少重复造轮子而保留独有能力 | candidate-mechanisms.md已覆盖16候选及Grok方法项；R3.1实跑OPEN-SOURCE-SEARCH的原npm/PyPI组件并保留失败语义，选择trafilatura为受限候选 | 完整候选产品与跨任务净收益仍未验证；registry原始正文、一个native批次及完整部署质量证据缺失 | 新任务暴露现有入口未覆盖能力，或出现有实质增量的新候选 | 只对能改变决定的候选做原件核对和一项最小真实运行，保留未验证边界 |
| last30days按需整体调用能保留更广社区证据 | R2 N1一次自然调用保留18条记录、排名外记录与partial/嵌套状态；T2两臂互补 | 只有有限近期样本，不能推出一般召回或成本优势；内部请求成本仍不可见 | 新任务确实需要近期跨社区证据，且低排名或部分失败可能改变决定 | 运行一次有明确决定缺口的近期任务，检查低排名关键项、完整索引和源级失败是否进入行动依据 |
| websearch完整检索栈值得恢复 | 0.6.1已部署，原搜索失败、DNS保留地址被安全检查拒绝；提取/分页组件可用 | 当前网络下原检索端到端能力 | 有安全允许的网络/上游修复证据 | 先查故障变化，再运行有限真实查询，不关闭安全检查 |
| 自主DeepResearch编排/付费公开设计可贡献不可替代能力 | LangChain/Jina等公开设计可读，完整运行缺独立授权LLM/API | 净收益、成本、依赖与访问边界 | 可用免费/现有明确授权入口，或公开设计能解决已观测缺口 | 锁定单机制与同题对照；缺授权仅暂停相关分支，不购买 |
| 持久研究/结构反思能改善后续行动 | research-skill完整存储/取回已跑；Deep Discovery100问已跑 | 对普通任务净收益未证，长流程可能浪费 | 重复提问/真实冲突或复杂决定出现 | 比较一次复用与一次新检索，或短反例审查与100问，不默认长流程 |
| 新成熟开源项目具有旧候选缺失能力 | 允许持续发现，不把旧名单当全集 | 当前新候选及实质增量未知 | 每轮候选调查或新任务暴露缺口 | 有限公开检索→原件核对→一项最小真实运行，有增量再部署 |

## 持续协作约束

长期 FS01 使命与单轮版本验收分开：R2/R3.1 pass 保留，但使命有可执行工作时保持 executing，下一步明确具体能力与执行者。B2 的真实缺口触发条件仅限制其自然发现实验，不扩散到其他候选；Home不等待用户另提任务或每日heartbeat才续接。每轮结束主动选下一项有证据价值的最小实际部署/复现，不为延续而编造改动或反复修订旧证据。

Home维护本路线图/合同/canonical；Execution独占当前实现与运行目录；Critic仅审查和写独立验收证据。任务ID以canonical为准。每日续接由来源配置，Home不重复创建；续接先检查在途任务，原轮未结束不另开并发修改。无归档或长期知识库写入授权。

## 2026-09-08 新任务接管与下一轮

唯一Home：[internal-id-omitted]；Execution：[internal-id-omitted]；Critic：[internal-id-omitted]。E/C returnTo 均为新Home，Critic reviews 为新Execution。旧Home [internal-id-omitted]、旧E1 [internal-id-omitted]、旧C1 [internal-id-omitted] 已停止写入；历史验收与配置原样保存在canonical migrationHistory及原证据，不转记为新任务执行。

当前固定配置（2026-09-08迁移后，覆盖全部历史模型约定）：Home gpt-6-astra/low；Execution gpt-5.6-luna/max；Critic gpt-6-astra/low。禁止自动升降档或沿用旧默认。跨任务消息一律省略model/thinking以保持接收任务已有配置，除非用户再次明确修改。

Home独占现有合同、路线和canonical；E2独占本轮实现与运行证据；C2独立核查决定性原证据并写自身验收记录。交接只给目标、已完成、未完成/失败、关键证据定位、下一步及判据；机械核对复用脚本，已通过且未受影响的检查不重复。来源只讨论需求，本次迁移确认后不收日常回执。现有fs01 heartbeat由来源迁到新Home，仍每日10点；不重复创建，不归档旧任务。

R8目标：补齐已有arXiv摘要搜索尚未证明的参考文献关系追溯能力。首选Crossref公开REST，先核验当前官方访问、字段和许可说明，再冻结一项种子论文任务及对照。最多查看5条参考记录、解析其中2条DOI，不递归遍历；原生搜读对照与API路线使用同一任务和明确预算。保存实际请求、原始响应、失败与缺字段状态，判断新增关系是否改变下一篇阅读选择；引用关系不代表支持、质量或已读全文。Semantic Scholar只作一次有依据的替代，保留限流/访问失败；不得称完整AI4Scholar复现。

本轮先在output/field-search-reproduction/runs/r8隔离实跑并交C2验收；有增量和许可依据才决定后续最小接入，不预先安装、不扩张自动路由。无增量允许真实负结果。R7.1已验收公开显式有限分页候选；R6.3仍本地内部显式候选；R2-v2.1及R4.1已有安装验收边界保留。B2仍等真实native gap，不为验证制造任务。长期使命保持executing。

## R8采用与R9下一步（2026-09-08）

R8由C2以FS01-R8-v1接受有界负观察及不部署。13/13原始bytes/hash核验；Crossref该种子缺reference，但原生PDF确有参考表；Semantic Scholar一次DOI查询未找到，服务回显正确解码标识，无证据认为编码错误，不机械重试或推断服务不可用。两篇摘要在API后读取，推荐仅为事后待读判断；0 DOI解析、未满足全部冻结成功门槛、无明确两臂时间预算，故不作因果/效率或行动质量排名。现有capture命令序列不等同精确历史重放，以manifest和原生记录为准。引用追溯整体能力仍未验证，非完整AI4Scholar复现。证据：runs/r8/RESULT-PACK-r8.md 与 runs/r8/critic/FS01-R8-v1/VERDICT-PACK.md；旧证据及installed Skill保持原样。

R9选择官方文档站点级发现：优先复用Trafilatura的sitemap_search成熟能力，USP为有具体理由时的替代。现有单页document读取不等于已验证站点发现。E2先冻结一个真实文档定位问题及预算，在原生搜读后记录未解条件，再有限实跑同源sitemap并读至多两个决定性页面；有实际覆盖/行动增量才提出最小接入，无增量允许不部署。不爬整站、不强造遗漏，不把URL数当质量或自建全站搜索框架。Home继续协调E2/C2，模型固定与写入分工保持不变。

## R9.1采用与R10方法调整（2026-09-08）

C2以FS01-R9.1-v1接受加性更正，R9-C1闭合；6项旧证据绑定、原包及manifest hash一致。该根sitemap确为八个版本URL的urlset，不是被max_sitemaps截断的index；只接受该次无新增页面与不部署。2次为外层调用，实际逐跳/redirect/retry数量与硬6请求预算仍未验证，不认证零重试或预算合规。原证据不覆盖，已通过项不重跑。证据：runs/r9/RESULT-PACK-r9.1.md、runs/r9/critic/FS01-R9.1-v1/VERDICT-PACK.md。

R8/R9连续无接入增量，R10先调整选题，不继续随机单接口试验：E2从现有入口实际缺口发现并筛选最多两个成熟、可合法免费运行的方案，优先用户核心的一手实践证据“发现→原文/线程读取→条件及失败保全”完整工作流。先写清缺口、已有路线不满足的原证据和候选独有能力，再选择一个端到端实跑；若已有路线足够，复用而非强造增量。方法型Skill不能以单采集器代表完整流程，不把调用成功、URL数量或文档审阅当持平。有限调研后仍无可运行高价值候选则回传明确证据与方向，Home据此调整，不盲目制造更多负例。保持同一E2实施、C2独立验收，固定模型及全部既有权限边界。

## R10采用与R11现有入口改进（2026-09-08）

C2 FS01-R10-v1接受本地人工显式第三方归档读取的条件增量。native已提供线程标题/月及反例；增量仅为可归属正文和时间字段，不是独立发现或性能复现。只有两条顶层回复、无more，第二回复晚于post归档；计数差异可由时点解释但非完整性证明。官方原件支持入口前提，PID/CPU/粒度是诊断建议，真正原因未确定。许可证、服务版本、原平台当前状态、深树/partial及可复用reader均未认证，继续不安装/自动/外发。不为演示发现重复标题/月搜索。证据：runs/r10/RESULT-PACK-r10.md、runs/r10/critic/FS01-R10-v1/VERDICT-PACK.md。

R11从实际观察转向现有统一入口的保全质量：installed integrated.py 的reddit-read读取上游top_comments并输出摘录与transport_failures；需由E2核查真实上游返回、选择/截断及失败语义是否已充分传递。若有决定性字段丢失，先在runs/r11隔离副本按现有模式最小修复并以原件/针对性反例验证；没有则不造字段/测试或框架。此轮不接Arctic Shift，不扩张路由，不修改installed Skill，候选通过C2后Home再决定部署。目标是改善用户可判断的完整性与失败保全，而非为了延续制造改动。

## R11.1候选验收与R11.2部署（2026-09-08）

C2 FS01-R11.1-v1接受候选，C1空解析与C2部分成功两项闭合。空样本不可仅凭reported总数判解析成功；records与failure并存保留有效insights/计数及失败；limit5下selected12/returned5/7insights成立。7条要点来自上游12条选择样本，非只总结最终5条；reported count非完整分母。search run ok而总体partial/degraded_sources的兼容传播已受控核验，invoke stub不是实网子进程证明。

R11.2由E2独占执行已授权可回退单文件部署：installed scripts/integrated.py前置42773cb69748a1cc03042eca8c5590c6668e1c2183da5cca2fb4f0bac3a1edf8，已验收候选f338f9b1664f87287162d4818f2e512d0c3d85e532efd16407430165dbd4de04。先核前置、备份原件、复制后核hash；任何漂移不覆盖。一次真实显式统一入口及安装入口C1/C2针对性受控复验，原HTTP响应未保全则明确仅最终输出证据。C2随后部署验收，当前不称部署已完成。其他Skill/上游/路由及R10受限候选不变。

## R11.2安装验收与继续方向（2026-09-08）

C2 FS01-R11.2-v1 accepted：限定安装部署通过。installed integrated.py=f338f9b1664f87287162d4818f2e512d0c3d85e532efd16407430165dbd4de04，原版备份=42773cb69748a1cc03042eca8c5590c6668e1c2183da5cca2fb4f0bac3a1edf8；search.py及upstream hash不变。恢复前必须同时重核当前后置及备份，避免覆盖后续修改。

真实installed search.py显式o0ln04 limit5原生记录与最终JSON一致：2 records/2 insights/num_comments2、非完整覆盖、无失败。只有最终JSON无原HTML，不认证原响应重算。安装路径绑定已独立确认，C1/C2为受控检查，partial并非实网复现。最终仅接受reddit-read统一字段保全、空解析保守状态及部分成功保全，不扩张完整覆盖、其他provider或自动路由。证据：runs/r11.2/RESULT-PACK-r11.2.md、runs/r11.2/critic/FS01-R11.2-v1/VERDICT-PACK.md。

下一轮R12先复用现有候选机制矩阵，有限筛选尚未完整复现且有实际增量的成熟工作流，不随机试孤立API，不重复已通过R11检查。E2交最多两项短名单，必须说明现有入口具体缺口、完整入口、许可/依赖和可改变决定的实跑判据；Home据证据定一项实施路径，再交C2独立验收。长期使命仍executing，本轮安装通过不等于使命完成。

## R12证据采用与R12.1隔离实现（2026-09-08）

C2 FS01-R12-v1接受受限证据：4份快照bytes/hash逐字节匹配原始HttpClient输出，3个seed出边/PID集合与Meta单实体精确对应，独立从R8 PDF核MoverScore、Mallen、BERT题名作者年份。creation=2023与ACL种子2024冲突未解释、BERT venue截短原样保留；不认证规范化完整。4只指外层调用，reGET为新观测，旧探针原字节缺失不回填，不作事前对照或新阅读选择优势。官方同日原件支持数据CC0、文档CCBY4、软件ISC及180/IP/min，不推SLA。

R12.1由E2在新runs/r12.1最小实现显式reader，先复用4冻结快照，不重抓已通过数据。保存raw/PID/OCI，精确绑定seed/cited/Meta身份，多实体保留歧义；失败与有效边并存partial；空不等于论文无引用，预算截断/未解析与覆盖未知分开。最多3个Meta、无递归/默认自动路由、不改installed或旧证据。必要实网入口至多一次Index请求并明确与回放区别。C2按决定性正常与反例核验后再由Home决定部署。

## R12.3限定候选验收与R12.4在线补齐（2026-09-08）

C2 FS01-R12.3-v1接受：Index来源失败与解析状态分离，403非空/空均unavailable，无效hash空为error，正常verified为ok/usable3/Meta3。解析候选和Meta保留不等于官方可用边。旧来源绑定、身份、超限与输出预检结论复用。仅离线Meta+可选单Index隔离候选通过，非部署或默认接入。

R12.4由E2在新目录补齐显式在线Index→最多3个Meta：复用已验收传输/解析器，不重试、不跟随重定向、不递归；Index不可用不发Meta，Meta失败保有效边并partial，预算未解析与覆盖未知分开。每响应保URL/时间/status/raw/hash及身份，Meta同样区分解析与来源可用性。新增链受控检查后唯一一次固定R8 seed真实reader运行，最多1 Index+3 Meta，不额外手工重抓。尚不修改installed，C2独立验收后再定最小接入。

## R12.4在线候选验收与R12.5显式接入（2026-09-08）

C2 FS01-R12.4-v1接受在线候选：原生session3447/3450唯一CLI live调用与新evidence/raw吻合，4响应独立解码/hash/URL/时间通过。可观察应用发送1 Index+3 Meta，无应用重试/redirect/认证，不推底层抓包计数。新增正常/Index失败阻断/Meta失败partial/去重预算/身份及JSON错误通过。旧来源/保全等复用。--live-index在未安装候选中成为完整链别名；接入须说明max-meta0仅Index。当前非安装验收。

R12.5由E2先隔离薄接入现有统一入口，显式调用固定DOI、max-meta0..3，普通默认read/search不触发。保留完整证据与partial/来源/预算/覆盖未知，必要Skill/provider说明同步，不新建框架。隔离入口受控验证通过后按准确文件hash/备份/漂移检查部署，保留R11.2及旧provider行为。部署后唯一一次真实installed新调用固定R8 seed、最多4应用发送，保原始证据；不得用候选实跑冒充安装实跑。C2随后独立安装验收，完成前不宣称已可用发布。

## R12.5安装采用与R13共通机制归纳（2026-09-08）

C2 FS01-R12.5-v1 accepted，显式有界在线引用边入口安装验收通过。当前五部署文件与post hash一致，reader为原R12.4；integrated保持R11.2 f338…de04。四个旧文件备份及integrated核对before hash；回退必须current==post且备份hash相符，未实际恢复。直接加载installed路径的六入口检查通过，普通默认路由不触发、预算0只Index、Index失败阻断、Meta失败保边partial/exit2和输出预检成立；doctor不联网。

唯一新installed search.py academic-edges实跑在12:17:50–12:17:56 UTC，原生调用3770/3773与输出/raw一一对应，1 Index+3 Meta均200、usable3/meta3。不是候选12:00运行。接受显式固定DOI入口，不包含默认自动路由、完整引用覆盖、引用即支持/质量、已读全文或持续可用保证。证据：runs/r12.5/RESULT-PACK-r12.5.md、runs/r12.5/critic/FS01-R12.5-v1/VERDICT-PACK.md。

R13回到长期使命的共通机制分析：E2只按R8–R12实际运行/验收提炼可复用机制与不可归约能力、已安装/受限候选/负观察/未验证状态和下一真实缺口，写一份加性紧凑摘要，不覆盖旧candidate-mechanisms，不为措辞再改Skill或造新测试。Home用它选择下一项有价值运行，不将单轮安装等同长期使命完成。

## 论文原文应用收尾与下一项选择（2026-09-08）

C2 FS01-R14-v1接受冻结目标/原版本的判断。RAGAs以retrieved context判断faithfulness，以question条件下句子占比判断context relevance；MoverScore原版比较reference，source-only在原文列为future work；BERT提供表示及任务层/特征路线。没有原文依据支持直接等价替代，不泛化所有衍生工具；无人工reference不等于无context，human judgments是验证标签，不是每次运行输入。校准/额外评分规则为实施推断。原文/hash/4文档获取动作已核，未推物理请求数。

现有引用边与原文读取组合足够支持本判断，无真实接口障碍，不新增代码、基准或第三篇，不把小切片契约核对变成验收前置。下一阶段回到成熟搜索方案选择：有限检查最多两个尚未完整复现的方法型Skill，优先可在现有宿主完成、能改善一手证据决策而非单接口采集的路径；Home先定价值和执行边界，不重复已完成学术种子/安装测试。用户沟通使用工作名称，轮次编号只保留内部追溯。

## 先行读取阶段收尾（2026-09-09）

C2 FS01-R15.1-v1接受仅本轮两页先行读取足够、不调用Crawl4AI。两快照content/raw hash及离线分页核验通过；准确来源为统一document的Jina第三方抽取，不是宿主native Web。官方README与quickstart决定条件可读，不据上游示例宣称installed wrapper全部功能或动态运行已验证。无新增代码/浏览器/部署测试；保持R4.1原限定验收。

当前E2正在有限发现最多两个新成熟搜索工作流，先交完整入口、独有增量与许可依赖证据，再由Home选择是否实跑。不重复已收尾两页或机械增加候选。

## 新候选处置收尾（2026-09-09）

C2 FS01-R16-v1接受两项暂不采用/不实跑。Agentic R&D 1.1.1的实际增量为修订指纹/stale失效/全局结构校验，与现有Home重叠；完整CLI仍需填artifact、推进gate及final，不能当三命令自动研究。dogfood自评分/13文档/用时不是产品质量证明。gh-aw完整Actions/engine/权限未具备；ASP.NET Core dry_run仍发评论，不能作只读试跑；类型lookup失败只阻断相关写入，不代表全部只读调查必须停止。Go源码构建条件非二进制运行必需，材料类型不等于来源独立性。

不新增安装、阶段门、评分体系或重复验证。只在真实版本回归问题出现时借鉴最后正常/首次异常及异根因反例，用现有只读流程。此轮没有剩余值得立即实施的候选，长期使命不标完成；下一推进必须由具体研究缺口或有独有增量的可运行能力驱动，不因heartbeat制造题目。当前没有在途实施/审查。原件及证据见runs/r16/critic/FS01-R16-v1/VERDICT-PACK.md。

R17（2026-09-09）已独立接受：仅将常驻 Windows/进程资源细节替换为已有 evidence.md 条件引用，16437→15844 bytes。只证明去重及静态可达，不证明token/账单/质量收益；quick_validate缺yaml未通过，未安装依赖。证据 runs/r17/RESULT.md 与 runs/r17/critic/VERDICT-FS01-R17-v1.md。E2/C2无在途写入，下一轮共享Skill写权交FS02 Home，FS01不得并发写入，待其收尾再协调。

R21-v2 已独立限定接受（2026-09-09）：显式Crawl4AI增加CSS动态等待/有界等待超时与可选链接、可能附件清单，复用原快照离线读取。公开延迟页面证明正文恢复；缺条件显式失败；auto保持Jina。签名链接由渲染HTML保真提取，离线反例与冻结arXiv HTML复核通过，附件仍not_fetched。保留运行环境与阶段超时限制、不承诺跨站完整性。证据 runs/r21/RESULT.md、runs/r21/critic/VERDICT-FS01-R21-v2.md。无在途写入；本轮停止，后续按整体能力路线选择，不继续局部扩展。

R22小红书（2026-09-10）最终installed-v2独立限定接受：显式search.py xiaohongshu薄接入固定上游只读search/feed，真实关键词/正文/互动及父子评论链通过；严格UTF8、敏感token/路径输出边界已修。依赖本机显式Python/adapter及用户扫码的独立session，不声称便携部署。评论target为加载目标非返回/网络硬上限，18条10顶层/has_more仅本次事实非完整采集；rc0/cleanup null非全树清理证明。默认公开路线/FS02/R21代码保护保持。证据runs/r22/INSTALLED-RESULT.md和critic/INSTALLED-VERDICT-v2.md。一次额外旧读取器离线测试因调用环境缺crawl4ai未运行成功，不冒称重跑通过。本轮结束，无在途写入，不继续局部打磨。
