> 历史文档共享副本 · 发布于 2026-09-17。保留当时的结论、失败、限制和计划；旧授权、自动续接、模型配置与“下一步”不作为当前执行指令。科研默认流程以 FS03 为准。已移除内部任务标识、替换本机路径并调整链接；没有重新运行历史验证。

# R22 最终 installed 验收：限定接受

唯一剩余阻断已关闭。安装 wrapper 现在递归遍历解析后 dict/list 中的字符串值，检测受保护路径，不再在 JSON 转义串上比较 Windows 原路径。

本轮独立离线检查直接路径、嵌套列表/字典中的路径均被拒绝，异常仅返回固定原因，不泄露路径；普通 partial_unknown 结果完整保留。未联网、启动浏览器、读取真实会话或重跑此前检查。证据见 `installed-checks-v2.json`。

wrapper SHA256 为 `2351233f14a71861bb7c9705a36b9f54aed6cd8d2d5e5b7e2c4251b27bb22353`，与目标及 installed provenance 一致。

结合此前已通过的显式路由、只读参数、严格 UTF-8、敏感输出、保护文件和 installed 脱敏实测证据，接受本轮最终 installed 薄接入。无剩余实质阻断，本轮结束。

接受范围仍限当前固定上游与已核实环境：运行依赖显式指定的 Python、适配器和已授权隔离会话。评论参数是 1..3 的加载目标，不是返回条数或网络条数硬上限；真实目标 1 返回 18 条、10 顶层且仍有更多评论，不能称完整采集。cleanup=null 不构成额外清理执行或全部浏览器进程独立验收；不扩展为任意站点或后续平台版本保证。
